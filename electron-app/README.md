# IPO Trademark Scraper Desktop (Electron)

## Dev

```bash
cd electron-app
npm install
npm run dev
```

## Notes

- The app runs the scraper via `python main.py --tm TM-XX` from the repo root.
- Exports and session summaries are read from `export/`.
